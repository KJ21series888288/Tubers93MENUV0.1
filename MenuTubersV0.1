local Players = game:GetService("Players")
local player = Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")

------------------------------------------------
-- GUI
------------------------------------------------

local Tubers93MENU = Instance.new("ScreenGui")
Tubers93MENU.Name = "Tubers93MENU"
Tubers93MENU.Parent = player:WaitForChild("PlayerGui")
Tubers93MENU.ResetOnSpawn = false
Tubers93MENU.DisplayOrder = 999999
Tubers93MENU.IgnoreGuiInset = true
Tubers93MENU.ZIndexBehavior = Enum.ZIndexBehavior.Global

------------------------------------------------
-- OPEN BUTTON
------------------------------------------------

local Open = Instance.new("ImageButton")

Open.Parent = Tubers93MENU
Open.Size = UDim2.new(0,100,0,80)
Open.Position = UDim2.new(0.5,-50,0,20)
Open.Image = "rbxassetid://140539405777351"
Open.BackgroundTransparency = 1
Open.ZIndex = 10

Instance.new("UICorner",Open)

------------------------------------------------
-- MAIN FRAME
------------------------------------------------

local Frame = Instance.new("Frame")
Frame.Parent = Tubers93MENU
Frame.Size = UDim2.new(0,650,0,360)
Frame.Position = UDim2.new(0.5,-325,0.35,0)
Frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
Frame.BackgroundTransparency = 0.2
Frame.Visible = false
Frame.ZIndex = 1

Instance.new("UICorner",Frame)

------------------------------------------------
-- TITLE
------------------------------------------------

local Title = Instance.new("TextLabel")
Title.Parent = Frame
Title.Size = UDim2.new(1,0,0,45)
Title.Position = UDim2.new(0,0,0,0)
Title.Text = "TUBERS93 MENU HACK"
Title.Font = Enum.Font.Bangers
Title.TextScaled = true
Title.TextColor3 = Color3.fromRGB(255,0,0)
Title.BackgroundTransparency = 1
Title.ZIndex = 5

------------------------------------------------
-- DRAG
------------------------------------------------

local dragging = false
local dragStart
local startPos

Title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = Frame.Position
	end
end)

UIS.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)

UIS.InputChanged:Connect(function(input)

	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then

		local delta = input.Position - dragStart

		Frame.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)

	end

end)

------------------------------------------------
-- BUTTON CREATOR
------------------------------------------------

local function createButton(text,pos,parent)

	local b = Instance.new("TextButton")
	b.Parent = parent
	b.Size = UDim2.new(0,150,0,40)
	b.Position = pos
	b.Text = text
	b.Font = Enum.Font.SourceSansBold
	b.TextScaled = true
	b.BackgroundColor3 = Color3.fromRGB(255,0,0)
	b.TextColor3 = Color3.new(1,1,1)
	b.ZIndex = 10

	Instance.new("UICorner",b)

	return b

end

------------------------------------------------
-- CATEGORY BUTTONS
------------------------------------------------

local PlayerCat = createButton("Player",UDim2.new(0,20,0,50),Frame)
local VisualCat = createButton("Visual",UDim2.new(0,190,0,50),Frame)
local CombatCat = createButton("Combat",UDim2.new(0,360,0,50),Frame)
local SongCat = createButton("Song",UDim2.new(0,520,0,50),Frame)

------------------------------------------------
-- TABS
------------------------------------------------

local function createTab()

	local tab = Instance.new("Frame")
	tab.Parent = Frame
	tab.Size = UDim2.new(1,0,1,-120)
	tab.Position = UDim2.new(0,0,0,120)
	tab.BackgroundTransparency = 1
	tab.ZIndex = 5

	return tab

end

local PlayerTab = createTab()
local VisualTab = createTab()
local CombatTab = createTab()
local SongTab = createTab()

VisualTab.Visible = false
CombatTab.Visible = false
SongTab.Visible = false

local function hideTabs()

	PlayerTab.Visible = false
	VisualTab.Visible = false
	CombatTab.Visible = false
	SongTab.Visible = false

end

PlayerCat.MouseButton1Click:Connect(function()
	hideTabs()
	PlayerTab.Visible = true
end)

VisualCat.MouseButton1Click:Connect(function()
	hideTabs()
	VisualTab.Visible = true
end)

CombatCat.MouseButton1Click:Connect(function()
	hideTabs()
	CombatTab.Visible = true
end)

SongCat.MouseButton1Click:Connect(function()
	hideTabs()
	SongTab.Visible = true
end)

------------------------------------------------
-- PLAYER TAB
------------------------------------------------

local InfJumpButton = createButton("Infinite Jump",UDim2.new(0,20,0,10),PlayerTab)

local SpeedBox = Instance.new("TextBox")
SpeedBox.Parent = PlayerTab
SpeedBox.Size = UDim2.new(0,150,0,40)
SpeedBox.Position = UDim2.new(0,200,0,10)
SpeedBox.PlaceholderText = "Speed"
SpeedBox.TextScaled = true
SpeedBox.BackgroundColor3 = Color3.fromRGB(255,0,0)
SpeedBox.TextColor3 = Color3.new(1,1,1)
SpeedBox.ZIndex = 10

Instance.new("UICorner",SpeedBox)

------------------------------------------------
-- VISUAL TAB
------------------------------------------------

local FullBrightButton = createButton("FullBright",UDim2.new(0,20,0,10),VisualTab)

------------------------------------------------
-- SONG TAB
------------------------------------------------

local VoiceButton = createButton("Tubers93Voice",UDim2.new(0,20,0,10),SongTab)
local VoiceLoopButton = createButton("Tubers93VoiceLoop",UDim2.new(0,200,0,10),SongTab)

------------------------------------------------
-- OPEN MENU
------------------------------------------------

Open.MouseButton1Click:Connect(function()
	Frame.Visible = not Frame.Visible
end)

------------------------------------------------
-- FULLBRIGHT
------------------------------------------------

local fb = false

FullBrightButton.MouseButton1Click:Connect(function()

	fb = not fb

	if fb then
		Lighting.Brightness = 3
		Lighting.ClockTime = 12
		Lighting.FogEnd = 100000
	else
		Lighting.Brightness = 1
	end

end)

------------------------------------------------
-- INFINITE JUMP
------------------------------------------------

local infJump = false

InfJumpButton.MouseButton1Click:Connect(function()
	infJump = not infJump
end)

UIS.JumpRequest:Connect(function()

	if infJump and player.Character then
		player.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
	end

end)

------------------------------------------------
-- SPEED
------------------------------------------------

SpeedBox.FocusLost:Connect(function()

	local num = tonumber(SpeedBox.Text)

	if num and player.Character then
		player.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = num
	end

end)

------------------------------------------------
-- SOUND
------------------------------------------------

local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://132225475210716"
sound.Volume = 5
sound.Parent = workspace

------------------------------------------------
-- PLAY ONCE
------------------------------------------------

VoiceButton.MouseButton1Click:Connect(function()

	sound.Looped = false
	sound:Play()

end)

------------------------------------------------
-- LOOP
------------------------------------------------

local loop = false

VoiceLoopButton.MouseButton1Click:Connect(function()

	loop = not loop

	sound.Looped = loop

	if loop then
		sound:Play()
	else
		sound:Stop()
	end

end)
